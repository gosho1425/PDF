/**
 * REMOVED – manual PDF upload is no longer supported.
 * PaperLens now uses folder-based ingestion only (see /ingest).
 * This file is kept as an empty stub so existing imports don't break the build.
 */
export {};
